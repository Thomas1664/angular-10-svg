import { ElementBaseDirective } from './element-base.directive';

describe('ElementBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementBaseDirective();
    expect(directive).toBeTruthy();
  });
});
