import { RectDirective } from './rect.directive';

describe('RectDirective', () => {
  it('should create an instance', () => {
    const directive = new RectDirective();
    expect(directive).toBeTruthy();
  });
});
